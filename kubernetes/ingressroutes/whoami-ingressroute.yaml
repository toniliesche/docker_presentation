apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: whoami-ingressroute

spec:
  entryPoints:
    - web
  routes:
    - match: Host(`whoami.phpug.hh`)
      kind: Rule
      middlewares:
        - name: middleware1
          namespace: default
      services:
      - name: whoami-service
        kind: Service
        port:
          name: web